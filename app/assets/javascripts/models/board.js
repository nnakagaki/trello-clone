TrelloClone.Models.Board = Backbone.Model.extend({
  urlRoot: "api/boards"
  
  // parse: function (resp) {
//     var lists =  resp.lists;
//
//     if (lists) {
//       this.set("lists", lists);
//       delete resp.lists;
//     }
  // }
})