<h1>My Boards</h1>
<ul>
  <% boards.sortBy("created_at").forEach(function (board) { %>
    <li><a href="#/boards/<%= board.id %>"><%= board.escape("title") %></a></li>
    <button id="delete-board" data-id="<%= board.id %>">Delete Board</button>
  <% }) %>
</ul>

<button id="create-new-board">Create New Board</a>